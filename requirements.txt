# Minimum versions, not exact pins, so pip can pick wheels that match your Python
# version instead of trying to compile from source.
opencv-python>=4.10
numpy>=2.1
python-dotenv>=1.0
ultralytics>=8.3
torch>=2.0
transformers>=4.45
pillow>=10.0
torchvision>=0.20
num2words>=0.5
huggingface-hub>=1.5.0
# Moondream2 ONNX (optional — only needed for python main.py moondream)
moondream==0.0.5
onnxruntime==1.19.2
